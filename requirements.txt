# Reproducibility environment for "When the Benford law must fail".
# Versions used to produce the published numbers (Python 3.12.10).
numpy==2.4.6
scipy==1.17.1
# recompute_table.py additionally loads the public archives (CSV/XLSX):
pandas==3.0.3
openpyxl
